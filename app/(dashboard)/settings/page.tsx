export default function SettingsPage() {
  return (
    <section className="rounded-3xl border border-[color:var(--border)] bg-[color:var(--surface)] p-8 text-sm text-[color:var(--muted)]">
      <h2 className="text-2xl font-normal text-[color:var(--foreground)]">Configurações</h2>
      <p className="mt-4">
        Área reservada para gerenciar conexões, permissões e personalização de widgets. Implementaremos após concluir autenticação e integração com o Kommo.
      </p>
    </section>
  );
}
